/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <stdio.h>

int main() {
    int i;
    float km = 0.0;
    float totale = 0;
    int superati_target = 0;
    int errori = 0;

    for(i = 1; i <= 7; i++) {
        printf("Inserisci km giorno %d: ", i);
        scanf("%f", &km);

        if(km < 0) {
            printf("Valore negativo \n");
            errori++;
        } else {
            totale = totale + km;
            if(km > 5) {
                superati++;
            }
        }
    }

    printf("Totalì km: %f\n", totale);
    printf("Giorni piu di 5 km: %d\n", superati);
    printf("Valori non valido: %d\n", errori);

    return 0;
}